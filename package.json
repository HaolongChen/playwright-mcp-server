{
  "name": "playwright-mcp-server",
  "version": "1.0.0",
  "description": "Docker-based Playwright MCP server with multi-platform deployment support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "playwright",
    "mcp",
    "docker",
    "automation",
    "browser"
  ],
  "author": "Haolong Chen",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "playwright": "^1.40.0",
    "winston": "^3.11.0",
    "@modelcontextprotocol/sdk": "^0.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
